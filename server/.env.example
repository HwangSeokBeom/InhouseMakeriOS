# TODO: Copy to .env for shared local defaults.
APP_ENV=development
PORT=3000
GLOBAL_PREFIX=
HEALTH_CHECK_PATH=/health

# TODO: Add database or third-party secrets when the real server is wired in.
# DATABASE_URL=

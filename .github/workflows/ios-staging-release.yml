name: iOS Staging Release

on:
  push:
    branches:
      - staging
    paths:
      - "InhouseMakeriOS/**"
      - "InhouseMakeriOS.xcodeproj/**"
      - "fastlane/**"
      - "scripts/ios/**"
      - "Gemfile"
      - ".github/workflows/ios-staging-release.yml"
  workflow_dispatch:

concurrency:
  group: ios-staging-release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release:
    if: ${{ github.ref_name == 'staging' }}
    runs-on: macos-latest
    environment: ios-staging
    env:
      FASTLANE_SKIP_UPDATE_CHECK: "1"
      FASTLANE_DISABLE_COLORS: "1"
      IOS_FASTLANE_APP_IDENTIFIER: com.hwb.InhouseIOS
      IOS_API_BASE_URL_STAGING: ${{ secrets.IOS_API_BASE_URL_STAGING }}
      IOS_GOOGLE_CLIENT_ID_STAGING: ${{ secrets.IOS_GOOGLE_CLIENT_ID_STAGING }}
      IOS_GOOGLE_REVERSED_CLIENT_ID_STAGING: ${{ secrets.IOS_GOOGLE_REVERSED_CLIENT_ID_STAGING }}
      IOS_DEVELOPMENT_TEAM: ${{ secrets.IOS_DEVELOPMENT_TEAM }}
      APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
      APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
      APP_STORE_CONNECT_API_KEY_CONTENT: ${{ secrets.APP_STORE_CONNECT_API_KEY_CONTENT }}
      BUILD_NUMBER_OVERRIDE: ${{ github.run_number }}
    steps:
      - name: Guard release branch
        run: |
          set -euo pipefail
          [[ "${GITHUB_REF_NAME}" == "staging" ]]

      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"

      - name: Install Fastlane bundle
        run: bundle install --jobs 4 --retry 3

      - name: Validate release secrets
        run: bash scripts/ios/validate_release_env.sh staging upload

      - name: Generate CI xcconfig
        id: xcconfig
        run: |
          set -euo pipefail
          path="$(bash scripts/ios/create_ci_xcconfig.sh staging)"
          echo "path=${path}" >> "${GITHUB_OUTPUT}"

      - name: Upload staging build to TestFlight
        run: bundle exec fastlane beta_staging
        env:
          CI_XCCONFIG_PATH: ${{ steps.xcconfig.outputs.path }}

      - name: Upload build artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-staging-build
          path: build/ios/staging
          if-no-files-found: ignore
